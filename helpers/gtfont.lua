gtfont = {}

--gtfont.init(spi_device) - 初始化高通字体芯片
--传入值：
--userdata 仅支持spi device 生成的指针数据
--返回值：
--boolean 成功返回true,否则返回false
--例子：
---- 特别提醒: 使用本库的任何代码, 都需要 额外 的 高通字体芯片 !!
---- 没有额外芯片是跑不了的!!
--gtfont.init(spi_device)
function gtfont.init(spi_device) end