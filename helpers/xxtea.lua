xxtea = {}

--xxtea.encrypt(data, key) - 加密
--传入值：
--string data 待加密的数据
--string key 加密用的密钥
--返回值：
--string 加密后的数据, 失败返回nil
function xxtea.encrypt(data,key) end

--xxtea.decrypt(data, key) - 解密
--传入值：
--string data 待解密的数据
--string key 解密用的密钥
--返回值：
--string 解密后的数据, 失败返回nil
function xxtea.decrypt(data,key) end